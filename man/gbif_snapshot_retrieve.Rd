% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/r-gbif-snapshot-retrieval.R
\name{gbif_snapshot_retrieve}
\alias{gbif_snapshot_retrieve}
\title{Retrieve GBIF Occurrences}
\usage{
gbif_snapshot_retrieve(save_path, gbif_snapshot_path, taxonomy_list)
}
\arguments{
\item{save_path}{The directory where the filtered species and genus data will be saved.}

\item{gbif_snapshot_path}{The path to the GBIF snapshot, typically in Parquet format.}

\item{taxonomy_list}{A data frame containing taxonomy information, including classification and rank information.
The file must include the rank information (e.g., "species" or "genus").
For species taxa, the \code{taxonomy_list} must include the GBIF \code{usageKey} for each species.
For genus taxa, the \code{taxonomy_list} must include the \code{genus} name for each genus.}
}
\value{
None. This function saves the filtered and processed occurrence data as \code{.rds} files in the specified directory.
}
\description{
This function retrieves and processes GBIF occurrences from a snapshot located at a given path.
It filters the data based on taxonomy records (class and order), processes the records for each species
and genus, and saves the resulting data as \code{.rds} files in the specified save directory.
}
\examples{
\dontrun{
gbif_snapshot_retrieve("path/to/save", "path/to/gbif_snapshot", taxonomy_list)
}
}
